name: Action-based workflow
on: 
  push:

jobs:
  action-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: prinding folder
        run: ls